<h1>User info</h1>
<%= image_tag user.get_profile_image %>
<h2>
  <%= user.name%>
</h2>
<h2>
  <%= user.introduction%>
</h2>
 <%= link_to edit_user_path(user.id), class: "btn btn-outline-secondary btn-blok edit_user_path(user)" do %>
 <i class="fas fa-user-cog"></i>
 <% end %>
<h1>New book</h1>
<%= form_with model: book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>opinion</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>
